import java.util.ArrayList;
import java.util.Collections;

public class ComparableSorting {
    public static void main(String[] args) {
        ArrayList<Integer> al = new ArrayList<>();
        al.add(12);
        al.add(23);
        al.add(45);
        al.add(29);
        System.out.println(al);
        Collections.sort(al);
    }

    // If we have the Employee object then it have
    // name, id, City then the critaria of Sorting
    // is not fixed then we have to decide the critaria

}
